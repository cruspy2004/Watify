# Render Environment Variables
# Copy these to your Render dashboard under "Environment"
# IMPORTANT: Use IPv4 hostname for Supabase connection

NODE_ENV=production
PORT=10000

# Supabase Database (Use IPv4 hostname)
# Get this from: Supabase Dashboard → Settings → Database → Connection String (Direct connection)
DB_HOST=db.YOUR_PROJECT_REF.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres.YOUR_PROJECT_REF
DB_PASSWORD=YOUR_DB_PASSWORD

# Alternative: Use full connection string
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_DB_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# JWT Secret (IMPORTANT: Change this in production!)
JWT_SECRET=watify_super_secret_jwt_key_2024_change_in_production_very_secure_token
JWT_EXPIRE=7d

# Frontend URL (Update after deploying frontend)
CLIENT_URL=https://watify.vercel.app
